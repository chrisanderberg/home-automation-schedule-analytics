## Checklist

- [ ] Tests added/updated as needed.
- [ ] `go test ./...` passes in `aggregation`.
- [ ] Analytics checks pass (`ruff check --select D analytics/src` and unit tests).
- [ ] Docstrings added/updated for new or changed Python production functions/classes.
