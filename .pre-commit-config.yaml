repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff-check
        name: ruff-docstrings
        args: ["--config", "analytics/pyproject.toml", "--select", "D"]
