repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.10
    hooks:
      - id: ruff-check
        name: ruff-docstrings
        args: ["--config", "analytics/pyproject.toml", "--select", "D", "analytics/src"]
        pass_filenames: false
